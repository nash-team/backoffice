# Application Environment
# Values: development, staging, production
ENVIRONMENT=development

# Security Configuration
# Generate a secure secret key: openssl rand -hex 32
SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/backoffice_dev
# For testing
# DATABASE_URL=sqlite:///./test.db

# Server Configuration
# development: 127.0.0.1 (localhost only)
# production: 0.0.0.0 (all interfaces) - use with caution
HOST=127.0.0.1
PORT=8001

# Google Drive Configuration
GOOGLE_DRIVE_FOLDER_ID=your-folder-id-here
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_CREDENTIALS_PATH=credentials/google_credentials.json

# LLM Configuration
# Provider: openai, openrouter
LLM_PROVIDER=openrouter
LLM_API_KEY=your-llm-api-key-here

# Text Generation Models (for ebook content/stories)
# OpenRouter models: anthropic/claude-3.5-sonnet, openai/gpt-4o, google/gemini-2.5-pro
LLM_TEXT_MODEL=anthropic/claude-3.5-sonnet

# Image Generation Configuration
# Gemini via OpenRouter (recommended - uses same API key as text generation)
LLM_IMAGE_MODEL=google/gemini-2.5-flash-image-preview
#
# Alternative: OpenAI DALL-E (Requires separate API key)
# OPENAI_API_KEY=your-openai-api-key-here
# LLM_IMAGE_MODEL=dall-e-3

# OpenRouter Configuration
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Application Configuration
APP_NAME=Backoffice
APP_VERSION=1.0.0
DEBUG=True

# Testing Environment
TESTING=false

# Local Stable Diffusion Configuration (Optional)
# For running image generation locally (100% FREE, no API needed)
# Models will be downloaded from HuggingFace on first run
#
# RESOURCE REQUIREMENTS:
#   - Disk: 3-20GB for model weights (depends on model)
#   - RAM: 8GB minimum for SDXL-turbo, 32GB+ for FLUX models
#   - GPU: NVIDIA GPU with 6GB+ VRAM recommended (or set LOCAL_SD_USE_CPU=true)
#   - Dependencies: torch, diffusers, transformers, accelerate

# Model selection (default: stabilityai/sdxl-turbo)
# Options:
#   - stabilityai/sdxl-turbo (2.5GB disk, 8GB RAM, fast, good quality)
#   - black-forest-labs/FLUX.1-schnell (17GB disk, 32GB+ RAM, slow, excellent quality)
LOCAL_SD_MODEL=stabilityai/sdxl-turbo

# SDXL-turbo quality tuning
# Steps: 1-4 (fast), 6-8 (better quality, slower)
SDXL_TURBO_STEPS=6
# Guidance: 0.0 recommended for turbo models
SDXL_TURBO_GUIDANCE=0.0

# Force CPU usage (slower, but works without GPU)
LOCAL_SD_USE_CPU=false

# HuggingFace API token (required for gated models like FLUX)
# Get it from: https://huggingface.co/settings/tokens
HF_API_TOKEN=your-huggingface-token-here

# Replicate Configuration (RECOMMENDED for image generation)
# Get your API token from: https://replicate.com/account/api-tokens
# Model: black-forest-labs/flux-schnell
# Pricing: $0.003 per image (10-30x cheaper than alternatives!)
REPLICATE_API_TOKEN=your-replicate-api-token-here

# Google Gemini Configuration (Direct API - not via OpenRouter)
# Note: Imagen 3 not available in all countries via AI Studio
# Get your API key from: https://aistudio.google.com/app/apikey
# Pricing: ~$0.04 per image (via Vertex AI only)
#GEMINI_API_KEY=your-gemini-api-key-here
