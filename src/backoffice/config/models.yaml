# Model mappings for ebook generation
# Source of truth for provider selection

models:
  # Cover generation: colorful, full-bleed
  # Using Gemini via OpenRouter - Best quality for children's illustrations ($0.04/image)
  cover:
    provider: openrouter
    model: google/gemini-2.5-flash-image-preview
    supports_vectorization: false

  # Content page generation: B&W coloring pages
  # Using Gemini Direct - Professional line art quality
  coloring_page:
    provider: gemini
    model: gemini-2.5-flash-image
    supports_vectorization: false


    # Alternative: Local FLUX.1-schnell (FREE but HEAVY: 17GB RAM)
    #provider: local
    #model: black-forest-labs/FLUX.1-schnell
    #supports_vectorization: false
